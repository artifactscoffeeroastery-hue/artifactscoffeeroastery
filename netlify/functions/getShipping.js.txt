exports.handler = async function(event, context) {
  const API_KEY = "5259d0d16806499bb5148ab831b219a7";
  
  try {
    // Replace the URL with your actual shipping provider's API endpoint
    const response = await fetch(`https://api.shippingprovider.co.za/rates`, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${API_KEY}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        // Pass relevant order details here (e.g., destination)
      })
    });
    
    const data = await response.json();
    
    return {
      statusCode: 200,
      body: JSON.stringify({ rate: data.shipping_cost })
    };
  } catch (error) {
    return { statusCode: 500, body: JSON.stringify({ error: "Failed to fetch rate" }) };
  }
};